pipeline {
    agent any
    stages {
stage("Build") {
        steps {
            echo "Building application..."
            sh ant -f 'reports/result.xml.xml' -v
            }
        }
        }
        }
