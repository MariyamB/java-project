pipeline {
    agent any
    stages {
stage("Build") {
        steps {
            echo "Building application..."
            ant -f 'reports/result.xml' -v
            }
        }
        }
        }
